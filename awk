对于类似表格的数据文件， 可以用awk操作， 
awk是按列操作文件的。 
一般一个命令的输出是很多行的， 而且这些行按字段对其的， 
比如netstat命令输出每行都是按字段对其的， 可以认为输出是一个表格文件。 
有事需要去除特定的段， 可以用awk, 
有时需要对段进行运算， 如图统计处于timewait的tcp连接的个数， 也可以用awk, 
而且awk支持管道， 天生支持。  
比如列出某一时间段的文件， 都可以用awk, 
讲多个文件合并， 然后在对合并后的文件的字段做运算，也可以用awk. 
